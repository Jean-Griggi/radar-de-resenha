# Shared — Resenhômetro

Utilitários, constantes e tipos compartilhados entre `apps/api` e `apps/web` (categorias de rolê, reações, usuários públicos, feed, etc.).

Importar de `@resenhometro/shared`.
