services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: resenhometro
      POSTGRES_PASSWORD: resenhometro
      POSTGRES_DB: resenhometro
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
